#include <stdio.h>
#include "dog.h"

/**
 *new_dog - function that initialize a variable of type struct dog
 *@name: struct pointer
 *@age: struct pointer
 *@owner: struct pointer
 */

dog_t *new_dog(char *name, float age, char *owner)
{
	dog_t *doggy:
	if (!doggy)
		return (NULL);

	return (doggy);
}
