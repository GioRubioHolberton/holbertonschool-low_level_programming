#include "holberton.h"

/**
 * _abs - prints the sign of a number.
 *
 * @n: variable
 *
 * Return: 1 if it's positive
 */
int print_last_digit(int n)
{
	n = n % 10;
	if (n < 0)
	  {
		n = n * -1;
	  }
	_putchar(n);
	return (n);
}
