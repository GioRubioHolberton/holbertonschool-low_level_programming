#include "holberton.h"
/**
 * print_chessboard - spn function that gets the length of a prefix substring.
 *
 * @a: pointer
 *
 * Return: s
 */
void print_chessboard(char (*a)[8])
{
	int con;

	for (con = 0 ; s[con] ; con++)
	{
		if (s[con] == c)
			return (&s[con]);

	}

	if (s[con] != c)
		return (0);

	else
		return (&s[con]);


}
