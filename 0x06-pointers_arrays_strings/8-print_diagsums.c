#include "holberton.h"
/**
 * print_diagsums - spn function that gets the length of a prefix substring.
 *
 * @a: pointer
 *
 * @size: int
 * Return: s
 */
void print_diagsums(int *a, int size)
{
	int con;

	for (con = 0 ; s[con] ; con++)
	{
		if (s[con] == c)
			return (&s[con]);

	}

	if (s[con] != c)
		return (0);

	else
		return (&s[con]);


}
