#include "holberton.h"
/**
 * *_strstr - spn function that gets the length of a prefix substring.
 *
 * @haystack: pointer
 * @needle: pointer
 *
 * Return: s
 */
char *_strstr(char *haystack, char *needle)
{
	int con;

	for (con = 0 ; s[con] ; con++)
	{
		if (s[con] == c)
			return (&s[con]);

	}

	if (s[con] != c)
		return (0);

	else
		return (&s[con]);


}
