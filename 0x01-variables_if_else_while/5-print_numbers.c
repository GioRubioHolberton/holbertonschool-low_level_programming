#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - principal funtion
 * Determine the last digit of a number
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0 ; n <= 9 ; n++)
		printf("%d", n);
		putchar('\n');

	return (0);

}
